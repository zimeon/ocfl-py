name: GHitHub actions workflow for ocfl-py

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
      - uses: actions/checkout@v3
        with:
          submodules: true
          recursive: true
      - uses: ./.github/actions/run-tests
